# تحديث: 22 صنف + صور CNC + Next 14.2.35

## اللي اتعمل في النسخة دي

| # | التعديل | الحالة |
|---|---|---|
| 1 | **22 صنف في الكتالوج** (كانوا 6) | ✅ |
| 2 | **صور الـ CNC والماكينات** (5 صور) مضافة ومحسّنة | ✅ |
| 3 | **تحديث Next** 14.2.15 ← **14.2.35** (20 تصحيح أمني) | ✅ |
| 4 | **صور الخدمات مش باينة** — الشفافية 13% ← **42%** (62% hover) | ✅ |
| 5 | **الدرفلة اتشالت** والتنكات مكانها | ✅ |
| 6 | **كل الـ 11 خدمة** بقى ليها صورة حقيقية | ✅ |
| 7 | **تكامل CRM** (`/api/quote` ← VTiger Lead) | ✅ الكود جاهز — محتاج `.env.local` |

**البناء:** 62 صفحة، نظيف بلا أخطاء. كل الصفحات اتّجرّبت = **200**.

---

## الكتالوج — 22 صنف

### صناعي — حسب الطلب (8)
تنكات وخزانات · خطوط مواسير · سلالم سيفتي · منصات وقواعد · أنظمة صناعية متكاملة · تركيب خطوط إنتاج · سيور نقل · مبادلات حرارية

### مخزون وتجهيزات (14)
وصلات صحية · محابس · ألواح ولفائف · مواسير · فلنجات · قطاعات وبارات · مسامير ومثبتات · ترابيزات · كراسي · أرفف واستاندات · أحواض · أغطية وكفرات · هندريل · فلاتر ومصافي

**كل صنف له:** اختياراته الخاصة (خامة/مقاس/سُمك/نوع…) ← **اطلب عرض سعر** ← Lead في الـ CRM.

**البيانات حقيقية:** الخامات والأسماك والمقاسات من **نما** (أوامر شغل قسم IS0022)، والصور من **مكتبة المصنع** (التصنيف بتاعك — الصور المتعلّمة «جاهزة للرفع» بس).

---

## النشر

### 1) ارفع الـ zip
```bash
cd /root/egygroup
git pull
rm -rf project_new && unzip -oq EgyGroupHS.zip -d project_new
cp -r project/node_modules project_new/ 2>/dev/null
cp project/.env.local project_new/ 2>/dev/null
rm -rf project_old && mv project project_old && mv project_new project
cd project
```

### 2) ⚠️ بيانات الـ CRM (لو لسه متعملتش)
```bash
nano .env.local
```
```
VTIGER_URL=https://crm.stlixvalley.com
VTIGER_USERNAME=admin
VTIGER_ACCESS_KEY=المفتاح_من_My_Preferences
VTIGER_ASSIGNEE=19x753
```

### 3) ابنِ وشغّل
```bash
npm install
npm run build      # لازم يطلّع: Generating static pages (62/62)
systemctl restart egygroup
sleep 5
curl -sI https://egygrouphs.com/ar/catalog | head -1
```

### 4) بُصّ
```
https://egygrouphs.com/ar/catalog          ← 22 صنف بفلترة
https://egygrouphs.com/ar/catalog/tanks    ← التنك + الـ configurator
https://egygrouphs.com/ar/services         ← 11 خدمة بصورها (شاملة CNC)
```

### 5) جرّب الـ CRM
افتح صفحة التنك ← اختار مواصفات ← اكتب اسمك وتليفونك ← **ابعت الطلب** ← شوف الـ Lead في VTiger (Subject: «طلب عرض سعر — تنكات وخزانات»).

---

## لسه معلّق

1. **صور التنكات** — عالجتها ورفعتلك مقارنة قبل/بعد، **مستني حكمك**. (التصنيف بيقول 1 من 40 بس «جاهزة» — المكتبة نفسها فقيرة في التنكات، فيمكن نحتاج تصوير جديد).
2. **لوجوهات الماركات** على صور الأوربيتال — مش شايف الصور فمش عارف مكانها.
3. **بيانات التواصل** (تليفون/إيميل/عناوين) — لسه placeholders في `src/content/site.ts`.
4. **Next 16** — الثغرتين المتبقيتين محتاجينه، بس بيكسر الكود (params بقت async). محتاج جلسة لوحده.
5. **باقي الأصناف** لحد 100 (دلوقتي 22).
