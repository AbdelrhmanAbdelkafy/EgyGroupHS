# تحديث: الكتالوج + ربط عرض السعر بالـ CRM + صور الخدمات

## 1) اللي اتعمل

### 🔗 ربط طلب عرض السعر بالـ CRM (VTiger) — مباشر
- الطلب من الكتالوج بيروح لـ **`/api/quote`** ← بيعمل **Lead في VTiger** فورًا.
- **مش عن طريق الإيميل** — اتصال مباشر بالـ CRM (أضمن، ومفيش طلب بيضيع).
- **لو الـ CRM وقع لأي سبب** ← الطلب بيرجع تلقائيًا لمسار الإيميل (fallback). الطلب مش بيضيع أبدًا.

**الحقول متحقّق منها فعليًا على الـ CRM بتاعك** (اتعمل lead تجريبي `LEAD23986` واتمسح):

| حقل الموقع | حقل VTiger | القيمة |
|---|---|---|
| اسم العميل | `firstname` | (إلزامي) |
| التليفون | `phone` | (إلزامي) |
| الشركة | `company` | |
| البريد | `email` | |
| المنتج | `cf_2262` (Subject) | «طلب عرض سعر — تنكات وخزانات» |
| المواصفات المختارة | `description` | كل الاختيارات مرتّبة |
| المصدر | `leadsource` | **Website** |
| المسؤول | `assigned_user_id` | **19x753 = Call Center** |
| الدولة/التواصل/الصناعة | `cf_905`/`cf_1963`/`industry` | Egypt / Inbound / Manufacturing |

### 🖼️ صور الخدمات
كروت الخدمات بقت **بصور حقيقية** (خلفية بتوضح مع الـ hover): قص ليزر · لحام ليزر · أوربيتال · خطوط مواسير · تشكيل.

### ⚠️ تصحيح مهم: الدرفلة اتشالت
زي ما قلت — **مفيش درفلة**. اتشالت من: الخدمات، جدول الإمكانيات، الـ Ecosystem، الـ SEO، والـ schema.
ومكانها اتحطّت **«تنكات وخزانات»** كخدمة أساسية (وليها صفحة كاملة في الكتالوج).

---

## 2) خطوات النشر

### أ) ارفع النسخة الجديدة
```bash
cd /root/egygroup/project
# ارفع EgyGroupHS.zip وفكّه هنا (أو git pull)
```

### ب) اضبط بيانات الـ CRM (مهم — من غيرها الطلبات مش هتوصل)
```bash
cd /root/egygroup/project
nano .env.local
```
اكتب فيه:
```
VTIGER_URL=https://crm.stlixvalley.com
VTIGER_USERNAME=اسم_المستخدم
VTIGER_ACCESS_KEY=مفتاح_الوصول
VTIGER_ASSIGNEE=19x753
```

**مفتاح الوصول بتجيبه إزاي:** VTiger ← صورتك (فوق يمين) ← **My Preferences** ← انزل لـ **Access Key** ← انسخه.

### ج) ابنِ وشغّل
```bash
npm install
npm run build
systemctl restart egygroup
sleep 5
curl -sI https://egygrouphs.com/ar/catalog | head -2
```

### د) اختبر التكامل
```bash
curl -s -X POST https://egygrouphs.com/api/quote \
 -H "Content-Type: application/json" \
 -d '{"product":"تنكات","specs":[{"label":"النوع","value":"تخزين"}],"name":"اختبار","phone":"01000000000","locale":"ar"}'
```
- `{"ok":true,"lead":"LEAD…"}` ← **شغّال** (شوف الـ lead في VTiger وامسحه)
- `crm_not_configured` ← الـ `.env.local` مش مظبوط
- `crm_login_failed` ← المستخدم أو المفتاح غلط

---

## 3) الكتالوج — 6 منتجات

| المنتج | الفئة | الاختيارات |
|---|---|---|
| تنكات وخزانات | حسب الطلب | نوع · خامة · سُمك · سعة · تشطيب · إضافات |
| خطوط مواسير | حسب الطلب | خامة · قطر · لحام · طول · نطاق |
| سلالم سيفتي | حسب الطلب | خامة · نوع · ارتفاع · هندريل |
| منصات وقواعد | حسب الطلب | خامة · مقاس · حمل |
| ترابيزات استانلس | قياسي | خامة · سُمك · مقاس · رف |
| كراسي استانلس | قياسي | خامة · نوع · كمية |

**لإضافة منتج:** عدّل `src/content/catalog.ts` بس — الصفحة والـ sitemap والـ schema بيتولّدوا لوحدهم.

---

## 4) لسه معلّق

1. **باقي الـ 100 صنف** (دلوقتي 6): أنظمة صناعية، سيور، تركيب خطوط إنتاج، أرفف، فلاتر، أحواض، كفرات، هندريل، ميكسرات…
2. **تحسين الصور**: 224 من 370 متعلّمة «محتاجة تحسين خلفية وإضاءة» — أهمها التنكات (4224×5632).
3. **بيانات التواصل** — لسه placeholders.
4. **تحديث Next.js** — تنبيه أمني.
5. **لوجوهات الماركات** على صور الأوربيتال.
